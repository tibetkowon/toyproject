server:
  port: 8080

spring:
  h2:
    console:
      enabled: true

  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:tcp://localhost/~/test;AUTO_SERVER=TRUE
    username: sa


  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
